void binToDec() {
    char bin[33];
    int dec = 0;
    printf("\n--- Biner ke Desimal ---\n");
    printf("Masukkan biner: ");
    scanf("%s", bin);
    for (int i = 0; bin[i] != '\0'; i++)
        dec = dec * 2 + (bin[i] - '0');
    printf("Desimal = %d\n", dec);
}
